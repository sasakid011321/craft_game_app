{
  "name": "craft_game_app",
  "private": true,
  "dependencies": {
    "esbuild": "^0.19.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "scripts": {
    "build": "esbuild app/javascript/application.js --bundle --sourcemap --outdir=dist --loader:.js=jsx --loader:.jsx=jsx",
    "build:css": "echo 'No CSS build configured'"
  }
}
